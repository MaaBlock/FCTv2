#include "Chunk.h"
/*
Pipeline* Chunk::m_pl = nullptr;
void Chunk::render()
{
    for (int i = 0; i < 16; i++) {
        for (int j = 0; j < 16; j++) {
            for (int k = 0; k < 256; k++) {
                if (block[i][j][k])
                {*/
                    /*Box* box;
                    box = new Box(m_pl);
                    box->size(Vec3(1, 1, 1));
                    box->color(Vec4(1, 1, 1, 1));
                    Vec3 pos = Vec3(m_chunkPos.x + i, j, m_chunkPos.y + k);
                    box->setPosition(pos);
                    box->create();
                    m_pl->draw(box);
                    box->release();
                    *//*
                }
            }
        }
    }
}
*/